#first_string = input('First number: ')
#second_string = input('Second number: ')

#first_number = float(first_string)
#second_number = float(second_string)

#answer = first_number + second_number
#answer_string = str(answer)


# float them directly to save lines
first = float(input('First number: '))
second = float(input('First number: '))

print('thinking...')
#print('The sum is ' + answer_string)
print(first + second)
